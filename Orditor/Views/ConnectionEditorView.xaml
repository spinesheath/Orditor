<UserControl x:Class="Orditor.Views.ConnectionEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Orditor.ViewModels"
             mc:Ignorable="d" 
             x:ClassModifier="internal"
             d:DataContext="{d:DesignInstance Type={x:Type vm:ConnectionEditorViewModel}}">
    <Grid Background="Black">
        <TextBlock Text="{Binding SelectedText}"
                   Foreground="White"/>
    </Grid>
</UserControl>
