<UserControl x:Class="Orditor.Views.WorldView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:Orditor.Controls"
             xmlns:vm="clr-namespace:Orditor.ViewModels"
             mc:Ignorable="d"
             Background="Black" 
             d:DataContext="{d:DesignInstance Type={x:Type vm:WorldViewModel}}">
    <controls:Viewer Zoom="0.2">
        <controls:WorldDisplay Graph="{Binding Graph}"
                               Areas="{Binding Areas}"
                               Selection="{Binding Selection}"/>
    </controls:Viewer>
</UserControl>
